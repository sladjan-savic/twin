# Session init

**Always load first:** `1_system_context.md` — role, working style, expectations. Never skip.

1. Ask: "What ticket are we working on?" — accept ticket link, title, or description.
2. If domain context is not obvious from the ticket, ask: "Which domain context applies?" — list available files from `/Users/sladjan/git/twin/orientation/` and wait for selection.
3. If a relevant anchor exists in `/Users/sladjan/git/twin/anchors/`, load `identity` + `core` only and summarise state in one sentence.
4. Proceed to `3_ticket_analysis.md`.
